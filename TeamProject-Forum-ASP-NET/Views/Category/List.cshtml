@model List<TeamProject_Forum_ASP_NET.Entities.Category>

@{
    ViewBag.Title = "List";
}

<div class="container">
    <div class="well">
        <h2>All Categories - @Html.ActionLink("Create", "Create", "Category", null, new { @class = "btn btn-primary" })</h2>
        <div class="row">
            <table class="table table-striped table-hover col-sm-12">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Name</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var category in Model)
                    {
                        <tr>
                            <td>@category.Id</td>
                            <td>@category.Name</td>
                            <td>
                                @Html.ActionLink("Edit", "Edit", "Category", new { @id = category.Id }, new { @class = "btn btn-warning btn-xs" })
                                @Html.ActionLink("Delete", "Delete", "Category", new { @id = category.Id }, new { @class = "btn btn-danger btn-xs" })
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
